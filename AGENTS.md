# Agent Notes

## GitHub Push Authentication

For this Windows machine, GitHub push authentication may be recoverable from:

```text
%USERPROFILE%\Desktop\git.txt
```

Do not copy, print, commit, or log the API key/token itself. Use the file only as a local credential hint when GitHub push requires authentication.